# 算24点 (24 Point Game)

一个基于 Rust 后端和 React 前端的算24点游戏程序。

## 功能特性

- 🚀 Rust 后端 API 服务
- ⚛️ React 前端界面
- 🎨 现代化的渐变色界面设计
- 🔄 支持多种运算符（加减乘除、阶乘、乘方、开方、对数）
- 🎲 自动出题功能
- 📊 显示所有可能的解法

## 项目结构

```
calc24/
├── Cargo.toml          # Rust 项目配置和依赖
├── src/
│   └── main.rs         # Rust 后端 API 服务
├── web/                # React 前端应用
│   ├── package.json
│   ├── vite.config.js  # Vite 配置（包含代理设置）
│   ├── index.html
│   └── src/
│       ├── main.jsx
│       ├── App.jsx
│       ├── App.css
│       └── components/
│           ├── RangeSelector.jsx
│           ├── OperatorSelector.jsx
│           ├── NumberInput.jsx
│           └── Results.jsx
├── start.sh            # 启动脚本
└── README.md
```

## 快速开始

### 前置要求

1. **Rust**: 访问 [https://rustup.rs/](https://rustup.rs/) 安装
2. **Node.js**: 访问 [https://nodejs.org/](https://nodejs.org/) 安装（推荐 v18 或更高版本）

### 安装依赖

#### 后端依赖
```bash
# Rust 依赖会在首次运行时自动安装
cargo build
```

#### 前端依赖
```bash
cd web
npm install
cd ..
```

### 运行程序

#### 方式一：使用启动脚本（推荐）

```bash
./start.sh
```

启动脚本会：
1. 启动后端服务（端口 3001）
2. 等待后端就绪
3. 启动前端开发服务器（端口 5173）
4. 自动打开浏览器访问 `http://localhost:5173`

按 `Ctrl+C` 停止所有服务。

#### 方式二：手动启动

**终端 1 - 启动后端：**
```bash
cargo run
```

**终端 2 - 启动前端：**
```bash
cd web
npm run dev
```

然后在浏览器中访问 `http://localhost:5173`

## 技术栈

### 后端
- **Rust**: 系统编程语言
- **actix-web**: 高性能 Web 框架
- **actix-cors**: CORS 中间件
- **tokio**: 异步运行时
- **serde**: 序列化/反序列化

### 前端
- **React**: UI 框架
- **Vite**: 构建工具和开发服务器
- **Modern CSS**: 渐变、动画、响应式设计

## API 接口

### POST /calculate
计算给定数字的24点解法

**请求体：**
```json
{
  "numbers": "A,2,3,9",
  "range": "basic",
  "operators": ["+", "-", "*", "/"]
}
```

**响应：**
```json
{
  "solutions": [
    "(A + 2 + 3) * 9 = 24",
    "..."
  ]
}
```

### POST /generate
生成一个有解的24点题目

**请求体：**
```json
{
  "range": "basic",
  "operators": ["+", "-", "*", "/"]
}
```

**响应：**
```json
{
  "problem": "A, 2, 3, 9"
}
```

## 开发

### 后端开发
```bash
cargo watch -x run  # 需要安装 cargo-watch
```

### 前端开发
```bash
cd web
npm run dev
```

### 构建生产版本

**后端：**
```bash
cargo build --release
```

**前端：**
```bash
cd web
npm run build
```

## License

MIT
